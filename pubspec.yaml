name: launchbox
description: LaunchBox admin dashboard
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.71.0  # Update to a more recent version
  firebase_core: ^2.25.4
  firebase_database: ^10.4.5
  intl: ^0.18.1